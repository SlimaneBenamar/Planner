{% extends 'base.html.twig' %}

{% block title %}Salle{% endblock %}

{% block body %}
    <h1>Salle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salle.id }}</td>
            </tr>
            <tr>
                <th>IdSalle</th>
                <td>{{ salle.idSalle }}</td>
            </tr>
            <tr>
                <th>CodeSalle</th>
                <td>{{ salle.codeSalle }}</td>
            </tr>
            <tr>
                <th>CapaciteSalle</th>
                <td>{{ salle.capaciteSalle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('salle_index') }}">Retour à la liste</a>

    <a href="{{ path('salle_edit', {'id': salle.id}) }}">Modifier</a>

    {{ include('salle/_delete_form.html.twig') }}
{% endblock %}
