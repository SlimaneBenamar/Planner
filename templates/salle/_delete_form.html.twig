<form method="post" action="{{ path('salle_delete', {'id': salle.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet enregistrement?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salle.id) }}">
    <button class="btn">Supprimer</button>
</form>
