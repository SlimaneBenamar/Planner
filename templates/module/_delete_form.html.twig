<form method="post" action="{{ path('module_delete', {'id': module.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet enregistrement?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ module.id) }}">
    <button class="btn">Supprimer</button>
</form>
