{% extends 'base.html.twig' %}

{% block title %}Edit Seance{% endblock %}

{% block body %}
    <h1>Edit Seance</h1>

    {{ include('seance/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('seance_index') }}">Retour à la liste</a>

    {{ include('seance/_delete_form.html.twig') }}
    {% if erreur is not null %}
        erreur : {{erreur}}
    {% endif %}
{% endblock %}
